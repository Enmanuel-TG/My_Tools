{
  "name": "My_Tools",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "python": "pip install -r apps/Api/requirements.txt",
    "server": "uvicorn server:app --host 0.0.0.0 --port $PORT",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.6.3",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
